# 🏠 Локальный сервер мессенджера

Полностью локальный сервер с веб-интерфейсом управления. Все данные хранятся на вашем компьютере в SQLite базе данных.

## 📋 Требования

- **Python 3.8+** - [Скачать](https://www.python.org/downloads/)
- **Windows, macOS или Linux**

## 🚀 Быстрый старт

### Windows (Самый простой способ):

1. Откройте папку `local-server`
2. Дважды кликните на **`start-server.bat`**
3. Дождитесь запуска (при первом запуске установятся зависимости)
4. Панель управления откроется автоматически в браузере
5. **НЕ ЗАКРЫВАЙТЕ окно командной строки!**

**Вот и всё!** 🎉

### Альтернативный способ (Windows):
Если нужно только установить зависимости:
- Запустите `install-dependencies.bat`

### macOS/Linux:
1. Откройте терминал в папке `local-server`
2. Выполните: `chmod +x start-server.sh`
3. Запустите: `./start-server.sh`

### Вручную (если bat файлы не работают):
```bash
# Установить зависимости
python -m pip install -r requirements.txt

# Запустить сервер
python server.py

# Открыть в браузере
start http://localhost:5000
```

### ⚠️ Если Python не найден:
1. Скачайте Python с https://www.python.org/downloads/
2. При установке **обязательно** отметьте:
   - ✅ "Add Python to PATH"
   - ✅ "Install pip"
3. Перезагрузите компьютер
4. Попробуйте снова

## 🎯 Доступ к интерфейсам

После запуска сервера:

- **Панель управления**: http://localhost:5000
- **Мессенджер**: откройте `index.html` в браузере (из корневой папки проекта)

## 🔧 Панель управления

### Возможности:
- ✅ Просмотр статистики (пользователи, сообщения, каналы)
- ✅ Управление пользователями (просмотр, удаление)
- ✅ Просмотр последних сообщений
- ✅ Очистка всех сообщений
- ✅ Создание резервных копий БД
- ✅ Автообновление статистики каждые 5 секунд

### Скриншот панели:
```
┌─────────────────────────────────────────┐
│  📊 Панель управления                   │
├─────────────────────────────────────────┤
│  👥 Пользователей: 3                    │
│  💬 Сообщений: 15                       │
│  📢 Каналов: 2                          │
│  💾 Размер БД: 2.5 KB                   │
├─────────────────────────────────────────┤
│  Пользователи | Сообщения              │
│  [Таблица]    | [Таблица]              │
└─────────────────────────────────────────┘
```

## 💾 База данных

### Структура:
- **messenger.db** - SQLite база данных
- Автоматически создаётся при первом запуске
- Хранит:
  - Пользователей
  - Сообщения
  - Каналы
  - Администраторов каналов
  - Подписчиков
  - Закреплённые сообщения

### Резервное копирование:
1. Откройте панель управления
2. Нажмите "Создать резервную копию"
3. Файл сохранится как `messenger_backup_YYYYMMDD_HHMMSS.db`

### Восстановление:
```bash
# Остановите сервер
# Замените messenger.db на резервную копию
cp messenger_backup_20241213_120000.db messenger.db
# Запустите сервер снова
```

## 👥 Демо-пользователи

При первом запуске создаются 3 демо-пользователя:

| Логин | Пароль   | Имя              |
|-------|----------|------------------|
| admin | admin123 | Администратор    |
| user1 | 123456   | Пользователь 1   |
| user2 | 123456   | Пользователь 2   |

## 🔌 Подключение клиента

### 1. Обновите `client-config.js`:
```javascript
const SERVER_CONFIG = {
  serverUrl: 'http://localhost:5000',
  // ...
};
```

### 2. Добавьте скрипты в `index.html` (перед `</body>`):
```html
<script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
<script src="client-config.js"></script>
<script src="socket-client.js"></script>
```

### 3. Подключитесь после авторизации в `auth.js`:
```javascript
// После успешной авторизации
const user = JSON.parse(localStorage.getItem('currentUser'));

socketClient.connect(user.id)
  .then(() => {
    console.log('✅ Подключено к серверу');
    window.location.href = 'index.html';
  })
  .catch(error => {
    console.error('❌ Ошибка подключения:', error);
  });
```

### 4. Отправка сообщений в `app.js`:
```javascript
function sendMessage() {
  if (!currentChatId) return;
  
  const text = document.getElementById('messageInput').textContent.trim();
  if (!text) return;
  
  const message = {
    text: text,
    type: 'text',
    time: new Date().toLocaleTimeString('ru-RU', {hour: '2-digit', minute: '2-digit'})
  };
  
  // Отправить через WebSocket
  const chat = chats.find(c => c.id === currentChatId);
  socketClient.sendMessage(currentChatId, chat.userId, message);
  
  // Добавить локально
  messages[currentChatId].push({...message, sent: true});
  
  document.getElementById('messageInput').textContent = '';
  renderMessages();
}

// Подписаться на новые сообщения
socketClient.onMessage((data) => {
  const { chatId, message } = data;
  
  if (!messages[chatId]) messages[chatId] = [];
  messages[chatId].push({...message, sent: false});
  
  if (currentChatId === chatId) renderMessages();
  renderChatList();
});
```

## 🌐 Доступ из локальной сети

Чтобы другие устройства в вашей сети могли подключиться:

### 1. Измените в `server.py`:
```python
# Было:
socketio.run(app, host='127.0.0.1', port=5000)

# Стало:
socketio.run(app, host='0.0.0.0', port=5000)
```

### 2. Узнайте ваш IP:
**Windows:**
```cmd
ipconfig
```
Найдите "IPv4 Address" (например: 192.168.1.100)

**macOS/Linux:**
```bash
ifconfig
```
Найдите "inet" (например: 192.168.1.100)

### 3. На других устройствах используйте:
```javascript
const SERVER_CONFIG = {
  serverUrl: 'http://192.168.1.100:5000',
  // ...
};
```

## 🔒 Безопасность

### Текущая реализация:
- ⚠️ Пароли хешируются (SHA-256)
- ⚠️ Только локальный доступ по умолчанию
- ⚠️ Нет HTTPS (не критично для локальной сети)

### Для продакшена рекомендуется:
- Использовать bcrypt для паролей
- Добавить JWT токены
- Настроить HTTPS
- Добавить rate limiting
- Шифровать сообщения

## 📁 Структура файлов

```
local-server/
├── server.py              # Основной сервер
├── requirements.txt       # Python зависимости
├── start-server.bat       # Запуск для Windows
├── start-server.sh        # Запуск для macOS/Linux
├── README.md             # Эта инструкция
├── templates/
│   └── admin.html        # Панель управления
└── messenger.db          # База данных (создаётся автоматически)
```

## 🛠️ API Endpoints

### HTTP API:
- `GET /` - Панель управления
- `POST /api/register` - Регистрация
- `POST /api/login` - Авторизация
- `GET /api/users` - Список пользователей
- `GET /api/messages/<chat_id>` - Сообщения чата
- `GET /admin/stats` - Статистика
- `GET /admin/users` - Все пользователи (админ)
- `DELETE /admin/users/<id>` - Удалить пользователя
- `GET /admin/messages` - Все сообщения (админ)
- `POST /admin/clear-messages` - Очистить сообщения
- `POST /admin/backup` - Создать резервную копию

### WebSocket Events:
- `connect` - Подключение
- `disconnect` - Отключение
- `user_online` - Пользователь онлайн
- `send_message` - Отправить сообщение
- `new_message` - Новое сообщение (получение)
- `message_sent` - Сообщение отправлено (подтверждение)
- `user_status` - Статус пользователя изменился

## ❓ Troubleshooting

### Проблема: "Python не найден"
**Решение:** Установите Python с https://www.python.org/downloads/
При установке отметьте "Add Python to PATH"

### Проблема: "pip не найден"
**Решение:** 
```bash
python -m ensurepip --upgrade
```

### Проблема: "Порт 5000 занят"
**Решение:** Измените порт в `server.py`:
```python
socketio.run(app, host='127.0.0.1', port=5001)  # Используйте другой порт
```

### Проблема: "База данных заблокирована"
**Решение:** Закройте все подключения к БД и перезапустите сервер

### Проблема: "Не подключается клиент"
**Решение:**
1. Проверьте что сервер запущен
2. Проверьте URL в `client-config.js`
3. Откройте консоль браузера (F12) для ошибок
4. Проверьте что Socket.IO CDN загружен

## 📊 Мониторинг

### Логи сервера:
Все события отображаются в консоли:
```
✅ Client connected: abc123
📨 Message sent: user1 -> user2
❌ Client disconnected: abc123
```

### Статистика в реальном времени:
Панель управления автоматически обновляется каждые 5 секунд

## 🔄 Обновление

Для обновления сервера:
1. Остановите сервер (Ctrl+C)
2. Создайте резервную копию БД
3. Замените файлы сервера
4. Запустите сервер снова

## 💡 Советы

1. **Регулярно создавайте резервные копии** БД
2. **Не удаляйте messenger.db** - там все ваши данные
3. **Используйте панель управления** для мониторинга
4. **Проверяйте логи** при возникновении проблем
5. **Закрывайте сервер корректно** (Ctrl+C)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли сервера
2. Проверьте консоль браузера (F12)
3. Убедитесь что все зависимости установлены
4. Проверьте что порт 5000 не занят

## 🎉 Готово!

Ваш локальный сервер готов к работе. Все данные хранятся на вашем компьютере и никуда не передаются.

Приятного использования! 🚀
